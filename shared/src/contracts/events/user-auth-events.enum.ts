export enum UserAuthExchangeName {
  USER_CREATED = "user-auth.user-created.exchange",
  USER_UPDATED = "user-auth.user-updated.exchange",
}

export enum UserAuthRoutingKey {
  USER_CREATED = "user-auth.user-created",
  USER_UPDATED = "user-auth.user-updated",
}
