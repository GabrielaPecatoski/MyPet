export enum ReviewExchangeName {
  CREATED = "review.created.exchange",
  COMPLAINT_CREATED = "review.complaint-created.exchange",
}

export enum ReviewRoutingKey {
  CREATED = "review.created",
  COMPLAINT_CREATED = "review.complaint-created",
}
