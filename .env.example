# Copie este arquivo para .env e preencha os valores reais antes de subir a stack
# cp .env.example .env

# Segredo de assinatura dos tokens JWT
# Gere um valor seguro com: openssl rand -hex 32
JWT_SECRET=mypet_super_secret_change_in_production
